# NOTE:  This file is autogenerated by the `meteor npm run-script dockerize` command.  
# Feel free to edit it for your local installation; but don't try to submit PR requests against it.  

version: '2'
networks:
  default:
    external: false
services:
  app:
    restart: always
    image: minimeteor
    ports:
      - "80:3000"
    links:
      - mongo
    environment:
      - ROOT_URL=http://example.com
      - MONGO_URL=mongodb://mongo:27017/meteor    
      - METEOR_SETTINGS=##_METEOR_SETTINGS_##   
  mongo:
    image: mongo:latest
    command: mongod --storageEngine=wiredTiger
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data
